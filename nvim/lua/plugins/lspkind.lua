local url = "onsails/lspkind.nvim"

local main = "lspkind"

-- local cmd = {}

local priority = 1

local lazy = false

local enabled = true

-- local dependencies = {}

-- local opts = {}

-- local config = {}

-- local keys = {}

return {
  url = url,
  main = main,
  -- cmd = cmd,
  priority = priority,
  lazy = lazy,
  enabled = enabled,
  -- dependencies = dependencies,
  -- opts = opts,
  -- config = config,
  -- keys = keys,
}
